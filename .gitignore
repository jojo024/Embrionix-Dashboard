# ── Go ────────────────────────────────────────────────────────────────────────
/embrionix-dashboard
/embrionix-dashboard.exe
/dist/
*.test
*.out
coverage.out

# ── Node / Frontend ───────────────────────────────────────────────────────────
web/node_modules/
web/dist/
web/.env.local
web/.env.*.local

# Embedded UI: ignore the built frontend copied in for `go:embed`, but keep the
# committed placeholder so a backend-only `go build` always has something to embed.
internal/webui/dist/*
!internal/webui/dist/.gitkeep

# ── Runtime data ──────────────────────────────────────────────────────────────
/data/
/logs/
/documentations/
*.db
*.db-shm
*.db-wal

# ── Config overrides ─────────────────────────────────────────────────────────
configs/config.local.yaml
.env

# ── OS / Editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
